tell git's usefullness in tracking code and even conversations, if accept:
add in current readme: "yeah, done that" AND cd root AND run git init
ask user, who gets execution rights 
add in requirements.txt: git on - no llm commit OR 
